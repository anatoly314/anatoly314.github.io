# anatoly.dev

Personal blog built with Hugo and the PaperMod theme, hosted on GitHub Pages.

## Local Development

```bash
# Run dev server
hugo server -D

# Build for production
hugo --gc --minify
```

## Deployment

Automatically deployed via GitHub Actions on push to `master` branch.
